// PATHS FOR ROUTES

interface Paths {
  main: "/",
  cart: "/cart",
  product: "/:productId",
  page404: "*"
}

export const paths: Paths = Object.freeze({
  main: "/",
  cart: "/cart",
  product: "/:productId",
  page404: "*"
});