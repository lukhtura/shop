import { createUseStyles } from "react-jss";

const useDropdownCurrencySelectorStyles = createUseStyles({
  container: {
    position: "relative"
  }
});

export default useDropdownCurrencySelectorStyles;